import styled from "styled-components";

export const Items = styled.div`
  display: flex;
  align-items: center;
  margin-left: 8px;

  a {
    margin: 0 0.5rem;

    img {
      margin: 0;
      width: 24px;
    }

    // &:first-child,
    // &:last-child {
    // 	margin: 0;
    // }
  }
`;
