import styled from 'styled-components'

export const Items = styled.div`
	display: flex;
	align-items: center;

	a {
		margin: 0 0.5rem;

		img {
            margin: 0;
            width: 22px;
		}

		// &:first-child,
		// &:last-child {
		// 	margin: 0;
		// }
	}
`